'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("Click")}>
        Click
      </button>
    </div>
  )
}

export default AddToCart
